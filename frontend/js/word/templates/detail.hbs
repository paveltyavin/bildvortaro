{{# if can_edit }}
    <div class="edit btn btn-default">
        redakti
    </div>
    <div class="remove btn btn-default">
        forvisi
    </div>
{{/if}}
<div class="row">
    <div class="col-md-10">
        <h2>{{name}}</h2>

        <p class="description">
            {{description}}
        </p>
    </div>
</div>
